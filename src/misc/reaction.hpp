#pragma once

#include "cstdint"

namespace Misc {
	enum class Reaction : uint8_t {
		vape,
		melt,

		aggravate,
		spread,

		burning,
		superconduct,
		swirl,
		electroCharged,
		shattered,
		overloaded,
		bloom,
		burgeon,
		hyperbloom,
	};
}